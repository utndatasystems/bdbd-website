# Bavarian Database Day

Website for the Bavarian Database Day, with archived and current yearly versions at `/2025/` and `/2026/`.

Year-specific styles live in `assets/css/editions/<year>.scss` and are loaded after the shared `assets/css/main.scss` bundle.
